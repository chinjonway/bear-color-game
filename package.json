{
  "name": "bear-color-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cf:login": "wrangler login",
    "cf:deploy": "npm run build && wrangler pages deploy dist --project-name bear-color-game",
    "cf:dev": "npm run build && wrangler pages dev dist"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "vite": "^5.4.19",
    "wrangler": "^4.20.0"
  }
}
